.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
uv.lock.bak
debates.db
.DS_Store
# bench/results/*.json is intentionally NOT ignored: the committed subset
# bench run is evidence for the readme's accuracy/cost table. Scratch runs
# during development are just not `git add`ed.
